/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    // 1. Tailwind CSS plugin must be executed
    tailwindcss: {},
    // 2. Autoprefixer plugin is optional but good practice
    autoprefixer: {},
  },
};